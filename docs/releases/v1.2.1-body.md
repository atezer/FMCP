## v1.2.1 — 2026-04-01

### Bridge

- **`figma_search_components`:** Component summary now includes **`key`** (for `figma_instantiate_component` / `componentKey`). Fixes the search → instantiate workflow used by generate-screen and related skills.
- **`prepublishOnly`:** Runs `npm run build:local && npm run validate:fmcp-skills` before `npm publish` (Cloudflare Worker `build:cloudflare` is separate; published bins are `dist/local.js` / `dist/local-plugin-only.js`).

### Documentation

- **[docs/TOOLS.md](https://github.com/atezer/FMCP/blob/v1.2.1/docs/TOOLS.md)** and **[docs/TOOLS_FULL_LIST.md](https://github.com/atezer/FMCP/blob/v1.2.1/docs/TOOLS_FULL_LIST.md):** Aligned with `registerTool` inventory; `figma_search_assets`, `figma_get_code_connect`, and `figma_use` are **not** registered in this build; documented alternatives (`figma_execute`, `figma_search_components`, etc.).
- **[docs/FMCP_AGENT_CANVAS_COMPAT.md](https://github.com/atezer/FMCP/blob/v1.2.1/docs/FMCP_AGENT_CANVAS_COMPAT.md):** Section 3 — current vs planned tools.
- **[docs/FIGMA_USE_STRUCTURED_INTENT.md](https://github.com/atezer/FMCP/blob/v1.2.1/docs/FIGMA_USE_STRUCTURED_INTENT.md):** `figma_use` is a draft; use **`figma_execute`** today.
- **[docs/FMCP_ENTERPRISE_WORKFLOWS.md](https://github.com/atezer/FMCP/blob/v1.2.1/docs/FMCP_ENTERPRISE_WORKFLOWS.md):** Workflow wording updated for the above.

### Cursor skills (F-MCP)

- New and updated skills under `.cursor/skills/f-mcp/` (canvas ops, generate screen/library, a11y, visual QA, screen analyzer, ds-impact-analysis, `TOOL_MAPPING.md`, coordination updates). See [CHANGELOG.md](https://github.com/atezer/FMCP/blob/v1.2.1/CHANGELOG.md) and [SKILL_INDEX.md](https://github.com/atezer/FMCP/blob/v1.2.1/.cursor/skills/f-mcp/SKILL_INDEX.md).

### npm

- **Registry:** After `npm publish`, confirm with `npm view @atezer/figma-mcp-bridge version` → **1.2.1**. If it still shows **1.2.0**, publish has not completed for this scope (login / org access).

### Note

- Full delta: root [CHANGELOG.md](https://github.com/atezer/FMCP/blob/v1.2.1/CHANGELOG.md) section **\[1.2.1\]**.

### Next release (maintainers)

1. Update **CHANGELOG.md**; create **docs/releases/vX.Y.Z-body.md**.
2. `gh release create "vX.Y.Z" --title "vX.Y.Z" --notes-file docs/releases/vX.Y.Z-body.md --repo atezer/FMCP`
3. See [RELEASE_NOTES_TEMPLATE.md](https://github.com/atezer/FMCP/blob/v1.2.1/docs/RELEASE_NOTES_TEMPLATE.md).

---

### Türkçe özet

- **Bridge:** `figma_search_components` çıktısına bileşen `key` eklendi; `figma_instantiate_component` akışı tamamlandı. `prepublishOnly` artık `build:local` + skill doğrulaması (Worker derlemesi npm yayınını bloklamaz).
- **Dokümantasyon:** `TOOLS.md`, tam liste, Agent Canvas uyumu, enterprise workflow ve `figma_use` taslağı netleştirildi (FUTURE §7 kapanışı).
- **Skill’ler:** Yeni/güncellenmiş F-MCP skill seti repoda.
- **npm:** `npm view` şu an **1.2.0** gösterebilir; paket sahibi `npm publish` sonrası **1.2.1** doğrulanmalı.
- **Sonraki sürüm:** CHANGELOG → `docs/releases/v…-body.md` → `gh release create`.
