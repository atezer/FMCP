{
  "_comment": "F-MCP Bridge Cursor config şablonu. Proje kökünde .cursor/mcp.json olarak kullanılıyorsa göreli yol yeterlidir; global ~/.cursor/mcp.json için <CLONE-KÖKÜ> yerine mutlak yol yazın. Kontrol: node dist/cli/fmcp.js doctor",
  "mcpServers": {
    "figma-mcp-bridge": {
      "command": "node",
      "args": ["dist/cli/fmcp.js", "serve"],
      "env": {
        "FIGMA_PLUGIN_BRIDGE_PORT": "5454"
      }
    }
  }
}
